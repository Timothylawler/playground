import React, { Component } from 'react';
import PropTypes from 'prop-types'

class AnimatedStripeMenu extends Component {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

export default AnimatedStripeMenu;